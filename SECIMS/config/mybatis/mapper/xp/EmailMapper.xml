<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cuit.secims.xp.dao.EmailDao">

	<insert id="addEmailInfo" parameterType="Email">
		insert into t_email values(#{userId},#{receiveId},#{type},#{sendTime})
	</insert>
	
</mapper>